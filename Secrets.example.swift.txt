// Copy this file to Secrets.swift (gitignored) and fill in your own values.
// Get them from `turso db show tension-mirror` (URL) and
// `turso db tokens create tension-mirror` (token).
enum Secrets {
    static let tursoURL = "libsql://tension-mirror-<your-org>.turso.io"
    static let tursoToken = "paste-your-token-here"
}
